## 1. プロジェクト基盤

- [x] 1.1 プロジェクト名を確定し、`go.mod`（module名）を作成する
- [x] 1.2 パッケージ構成 `cmd/app`, `internal/{app,session,spatial,overlay,capture,input,monitor,settings,tray,tutorial,win32}` の雛形を作成する
- [x] 1.3 LICENSE(MIT) と README、`assets/`・`docs/` ディレクトリを用意する
- [x] 1.4 Per-Monitor v2 DPI Aware を有効化する（マニフェスト or `SetProcessDpiAwarenessContext`）

## 2. 共通データ構造（spatial）

- [x] 2.1 `Rect`/`Point`/`Key`/`Anchor`/`ClickAction`/`SelectionStage`/`Session` を定義する
- [x] 2.2 ラベルに割り当てる固定英字キー配列（25個・方向の意味なし）を定義する

## 3. Win32 隔離層（win32）

- [x] 3.1 `RegisterHotKey`/`UnregisterHotKey` ラッパを実装する
- [x] 3.2 対象モニター物理ピクセルの画面キャプチャを実装する
- [x] 3.3 レイヤード最前面・枠なしウィンドウ生成（`WS_EX_LAYERED|TOPMOST|TOOLWINDOW`）を実装する
- [x] 3.4 `SetCursorPos`/`SendInput`（左/右/ダブルクリック）ラッパを実装する
- [x] 3.5 モニター列挙・矩形・DPI取得（monitor連携）を実装する

## 4. キーボード入力（keyboard-input）

- [x] 4.1 左/右/ダブルの3ホットキー登録とID→`ClickAction` 対応を実装する
- [x] 4.2 ホットキー登録失敗時に設定画面を開く処理を実装する
- [x] 4.3 選択中の `WM_KEYDOWN` 仮想キー+`VK_SHIFT` 判定を実装する（IME/Caps Lock非依存）
- [x] 4.4 `Backspace`/`Esc` の捕捉と、無効キーの無視を実装する

## 5. キャプチャとセッション（capture / session-lifecycle）

- [x] 5.1 セッション開始時に対象モニターをメモリへ1枚キャプチャし、ファイル保存しない
- [x] 5.2 多重起動防止（セッション進行中の再起動を無視）を実装する
- [x] 5.3 状態機械（Idle→Capturing→Selecting→Zooming/Executing/Cancelled→Idle）を実装する
- [x] 5.4 セッション終了（クリック/キャンセル/エラー）時にキャプチャ画像を破棄する
- [x] 5.5 `recover` によるパニック捕捉→オーバーレイ破棄→Idle復帰、画像/ラベル列/座標を含まないログ出力を実装する

## 6. 空間選択アルゴリズム（spatial-selection）

- [x] 6.1 検索領域 `R` を `cols×rows` に等分し各中心へAnchorを生成する（重なりなし・同領域同配置）
- [x] 6.2 通常入力で `SourceArea` を次検索領域とし、新Anchorを生成する
- [x] 6.3 `SelectionStage` スタックと `Backspace` による段階復帰を実装する
- [x] 6.4 オーバーレイ座標→検索領域→元モニター物理座標 の変換（丸めは最終のみ）を実装する
- [x] 6.5 最大段階数（既定5）の制御と、最大段階での自動クリック禁止を実装する
- [x] 6.6 同一入力で同一座標に到達する決定論性のユニットテストを書く

## 7. オーバーレイ描画（overlay-rendering）

- [x] 7.1 対象モニター全体を覆う枠なし最前面オーバーレイを表示する
- [x] 7.2 初回キャプチャの `SourceArea` 切り出し・拡大描画を実装する（再キャプチャしない）
- [x] 7.3 グリッド線/十字線/照準を描画せず、半透明ラベルのみを描画する
- [x] 7.4 ラベル中心を `SourcePoint` に一致させ、明暗両対応の縁取り/ハロー描画を実装する
- [x] 7.5 選択後の不要ラベル即時消去と領域強調アニメーション（設定で切替）を実装する
- [x] 7.6 ラベルサイズ（小/標準/大/特大）切替を実装する
- [x] 7.7 画面端のステータス表示（クリック種別・段階）を実装する

## 8. クリック実行（click-execution）

- [x] 8.1 Shift付きラベルで「オーバーレイ非表示→カーソル移動→入力送信→セッション終了」を実装する
- [x] 8.2 左/右/ダブルクリックの送信を実装する（追加確定キー不要）
- [x] 8.3 `Esc` 時にカーソル位置・クリック状態を変更しない処理を実装する
- [x] 8.4 クリック送信失敗時に通知して終了する処理を実装する

## 9. 設定（settings-config）

- [x] 9.1 `%APPDATA%\<ProjectName>\config.json` の読み書きと既定値を実装する
- [x] 9.2 設定ファイル欠如時に既定値で起動する処理を実装する
- [x] 9.3 破損時に退避(`config.json.bak`)して既定値起動する処理を実装する
- [x] 9.4 GUI設定画面（ホットキー/ラベル数/最大段階/ラベルサイズ/アニメーション/自動起動/チュートリアル再表示/初期化）を実装する

## 10. 常駐・自動起動・チュートリアル（tray-autostart）

- [x] 10.1 タスクトレイ常駐（設定表示・終了メニュー）を実装する
- [x] 10.2 待機中に画面キャプチャ/高頻度ポーリングを行わないことを確認する
- [x] 10.3 レジストリ `Run` キーによるWindows自動起動の有効/無効切替を実装する
- [x] 10.4 初回チュートリアル表示と設定からの再表示を実装する

## 11. 統合・検証

- [x] 11.1 `app` でトレイ→ホットキー→セッション→クリックの全体配線を行う
- [x] 11.2 起動→初期ラベル p95 100ms、絞り込み→次ラベル p95 50ms を計測し満たす（未達ならDirect2D化を検討）
- [x] 11.3 Full HD/WQHD/4K で座標ずれがないことを検証する
- [x] 11.4 受け入れ基準1〜14を一通り確認する（グリッド非表示・任意段階クリック・Backspace/Esc・IME/Caps非依存・画像非保存/非送信 等）
